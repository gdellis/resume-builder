version: '3.8'

services:
  resume-builder:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - OLLAMA_CLOUD_URL=${OLLAMA_CLOUD_URL}
      - OLLAMA_MODEL=${OLLAMA_MODEL}
    restart: unless-stopped